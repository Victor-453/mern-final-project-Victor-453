# ===========================================
# BACKEND ENVIRONMENT VARIABLES (Render Backend Service)
# ===========================================

# MongoDB Connection String
# Local Development: Use your local MongoDB or Atlas connection string
# Production (Render): Add this in Render Dashboard > Environment Variables
MONGO_URI=mongodb+srv://your_username:your_password@cluster0.xxxxx.mongodb.net/cartify?retryWrites=true&w=majority

# JWT Secret Key (use a strong random string)
# Generate a secure key: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# Production (Render): Will be auto-generated by Render or set manually
JWT_SECRET=your_super_secret_jwt_key_here_change_this_in_production

# Server Port
# Local Development: 5000
# Production (Render): 10000 (Render's default)
PORT=5000

# Node Environment
# Local Development: development
# Production (Render): production
NODE_ENV=development

# Client URL (for CORS and Socket.io)
# Local Development: http://localhost:5173
# Production (Render): https://cartify-frontend.onrender.com (your Render frontend URL)
CLIENT_URL=http://localhost:5173
